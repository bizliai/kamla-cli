{
  "name": "hello",
  "description": "Sends a greeting message",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the person to greet"
      }
    },
    "required": ["name"]
  },
  "command": "echo 'Hello, {{name}}! Welcome to Kamla CLI!'"
}